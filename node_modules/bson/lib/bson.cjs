"use strict";
// Compatibility shim: some consumers require ./lib/bson.cjs but
// this package installation only contains other builds (e.g. bson.js, bson.mjs).
// Re-export the existing CommonJS-compatible bundle.
module.exports = require('./bson.js');
